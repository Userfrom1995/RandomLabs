// AUTO-GENERATED by `prism train-lift` (Route 8 learned nonlinear lifting).
// Editable ONLY by the trainer. All-zero offsets are the LeGall 5/3 fallback,
// so the codec is bit-exact-compatible with the PRIMARY filter until trained.
// NOTE (2026-08-29, the Builder): training on Kodak produced predict offsets of
// +/-4 that REGRESS the codec by ~+4.7% (3.2442 -> 3.4711 per-sample). The
// single global piecewise-corrector LUT is misapplied across decomposition
// levels and propagates a context-dependent shift into the low-pass band. The
// LeGall 5/3 fallback therefore remains the floor; see progress/130-prism-route8-learned-lifting.md.
#ifndef PRISM_WAVELET_LIFT_DATA_INC
#define PRISM_WAVELET_LIFT_DATA_INC
#include "prism/codec/wavelet.h"
namespace prism::codec {
inline LearnedLift baked_learned_lift() {
    LearnedLift L{};
    // pred_lut / upd_lut baked here by the trainer (currently LeGall fallback).
    return L;
}
}
#endif
