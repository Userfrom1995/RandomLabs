#include "prism/codec/cm.h"
namespace prism::codec {
// CM implementation lives in prism.cpp encode/decode generic helpers
// (expanded contexts). This translation unit ensures the header is
// compiled and linked.
} // namespace prism::codec
