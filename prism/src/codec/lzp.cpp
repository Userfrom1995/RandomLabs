#include "prism/codec/lzp.h"
namespace prism::codec {
// LZP implementation lives in prism.cpp encode/decode generic helpers
// (hash table + flag stream). This translation unit ensures the header is
// compiled and linked.
} // namespace prism::codec
