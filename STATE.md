# STATE - Random factory checkpoint
 - **Updated: 2026-09-24T07:18Z (maintainer run 35968926460 - issue_comment on PR #401 ea051956, main ce933b7a LIVE, routing fixes landed, staged drift blocks tor-cli, Lab on #399 re-dispatched)**

## STANDING OWNER DIRECTIVES (active)
 - **DOCUMENTATION INVARIANT DIRECTIVE (2026-09-24T06:20Z, via #397 CLOSED at fee11745):** Enforce Unified Documentation Invariant and abolish internal milestone leakage in agent prompts - codify invariant in `AGENTS.md`/`LAB.md` (no milestone/sprint leakage in `<project>/docs/` + `<project>/README.md`), semantic phase naming (descriptive vs M1/M2), update prompts for `architect`/`builder`/`reviewer`/`maintainer`/`curator`, Lab Engineer executor. Issue #397 CLOSED at fee11745, PR #398 MERGED fee11745 Refs #397 (5 commits) - invariant now live on main, historical progress/ledger untouched.
 - **TOR CLI DIRECTIVE (2026-09-23T19:14:33Z, via #42):** Build lightweight, fast, cross-platform open-source Tor CLI - `tor-tool <app> [args]` per-app only, `tor-tool shell` isolated shell, `sudo tor-tool connect`/`disconnect` system-wide with safe restore, `status`/`version`, use existing Tor (auto-manage/detect, startup/readiness/reuse/cleanup/Ctrl-C/failures/DNS/IPv4/IPv6, never falsely claim protected), Linux/macOS/Windows behind common CLI, thorough GitHub Actions testing across 3 OS. Tracking issue #387 OPEN, M1..M5 all MERGED Refs #387, Lab CI + repairs + final hardening + staged sync MERGED 228d7cb9 Refs #387 (byte-identical staged/installed 9.9 eval, 6/6 tri-OS on 4098d08). Deploy success on 228d7cb9 35962553419 and on fee11745 35965214683 success. Manual tor-cli dispatch 35968005834 success on fee11745 (live-head green before drift). Issue #387 awaits tri-OS green on live head + per-OS testers before Closes. User field failure 2026-09-24T05:44Z + per-OS tester request 2026-09-24T06:57Z.
 - **UMBRA DIRECTIVE (2026-09-22T15:43Z, via #375 CLOSED):** Shadow Fight-inspired WebGPU Combat at `/umbra/` - 7 gates COMPLETE.
 - **DOOM DIRECTIVE (2026-09-17T21:10Z, via #362 CLOSED):** Client-Side Web Doom at /doom/ - 6 gates, issue #362 CLOSED at e33e11f1 (M1-M5 complete).
 - **POOLDUEL CLOSED (2026-09-16T16:56Z, via #302):** Owner closed #302; REDESIGN terminated.
 - **LAB RIGOR GATES (2026-09-07T15:47Z):** Brutal rigor charter live.
 - **EXCELLENCE IN CRAFTSMANSHIP CHARTER (2026-09-04T16:28Z):** Live on 61b09c8.
 - **LAB HEALTH NOTICE (2026-09-07T16:06Z via #70):** RESOLVED - README.md and index.html now accurately reflect live main.

## CRITICAL INFRASTRUCTURE STATE
 - **Main ce933b7a LIVE - drift blocks green:** `origin/main` = `ce933b7a7c1d45ca8c22b2c6eafea1f92eb9c7ca` verified via `git ls-remote origin/main` == ce933b7a and `gh api repos/Userfrom1995/RandomLabs/git/refs/heads/main --jq .object.sha` == ce933b7a (parent fee11745, merge PR #400 `lab: fix tor-cli sweep gap on main (allowlist + verification + schedule backstop)` Refs #399, 2 commits). `git log --oneline -5 origin/main` = ce933b7a / fee11745 / 057033cb / 495d49ba / 90b48666. `git merge-base origin/main ea051956` present (non-orphan, PR branch rebased via Lab). `tor-cli` push on ce933b7a `35968359503` failure: Hermetic `ubuntu/macos/windows` FAIL `TestTesterStagedCIByteIdenticalWithInstalled` (9842 vs 9979 bytes) while `cross-compile` success + `fuzz` pass + `live-tor` in_progress/pending; pull on `ea051956` `35968911359` same 3x Hermetic fail (9842 vs 9979 on linux/macos, 10099 vs 10240 on windows CRLF) . `tor-cli` at `fee11745` was green via `35968005834` dispatch before drift. `maintainer.yml` workflows 17/17 PASS on main (includes `tor-cli`, `opencode-peros-test` pending via PR #401 merges). `opencode.json` two-knob free (`muse-spark-1.3-contributor-free` / `muse-spark-1.2-contributor-free`, evaluator `muse-spark-1.3-contributor-free`). No `workflows permission` rejection on Lab PAT push `35968780203`.
 - **PR #401 per-OS testers at ea051956:** Branch `opencode/lab-387-tor-cli-ci` `ea051956` (5 commits: `7b7284b3` prompts, `9265d184` workflow, `2d3edc5a` registry+docs, `d09aab4a` tor-cli skip-trim, `ea051956` routing fixes) MERGEABLE `UNSTABLE` on `ce933b7a`. Lab Engineer push `35968780203` success via PAT (2d3edc5 -> ea051956, forward `review`). Reviewer pending/in_progress at 07:18Z (`35968915563` in_progress, `35968926518` pending) on `ea051956` - verifies 2 blocking fixes (prefix guard at `opencode-test.yml:13`, forward guard at `opencode-peros-test.yml:93/178/266`). Prior Lab on #399 `35968615980` success at 07:15Z but left staged drift unsynced. No `Co-authored-by` trailers, no em dashes, no PAT in agent env.
 - **Tor CLI epic position:** Staged sync MERGED at `228d7cb9` (9.9 eval, 6/6 on `4098d08` byte-identical). Sweep gap fix MERGED `d3fabf5a+ce933b7a` Refs #399 (allowlist adds `tor-cli`, verification polling 6x30s, `workflow_call`+`schedule` backstop). Drift introduced: `.github/workflows/tor-cli.yml` 9979 bytes (with `workflow_call`/`schedule`) vs `tor-cli/ci/tor-cli.yml` 9842 bytes (old header + missing triggers) on main; on branch `9842` installed vs `10456` staged (skip-heavy staged from `d09aab4a` retire). `TestTesterStagedCIByteIdentical` now red on every OS. Per-OS tester workflow `opencode-peros-test.yml` added (303 lines, 3 jobs `ubuntu/macos/windows` native, `cancel-in-progress: false`, `shell: bash` on windows, `GITHUB_TOKEN` only). Docs synced at 7 locations.
 - **Documentation Invariant epic position:** Issue #397 CLOSED at fee11745, invariant live.

## IN FLIGHT
 - **Tor CLI #387 OPEN - live-head drift blocks Closes:** Issue #387 OPEN, M1-M5 + Lab + hardening + staged sync all MERGED Refs #387, Evaluator 9.9 on staged sync, manual sweep `35968005834` green on `fee11745`, sweep fix MERGED `ce933b7a` Refs #399, but staged drift blocks tri-OS green on live head `ce933b7a`. Per-OS tester PR #401 at `ea051956` OPEN MERGEABLE with routing fixes landed, review pending at 07:18Z, `tor-cli` pull `35968911359` running (3 Hermetic fail pending final). `Refs #387` retained.
 - **Audit #399 OPEN - staged sync regression, Lab re-dispatched:** Issue #399 OPEN, PR #400 MERGED `ce933b7a` Refs #399 but drift `9842 vs 9979` blocks `tor-cli` 6/6 on `ce933b7a` and `ea051956`. Lab Engineer on #399 dispatched at 07:15:09Z (`35968615980` success but drift unverified) - this run re-dispatches Lab on #399 at 07:18Z to `cp .github/workflows/tor-cli.yml tor-cli/ci/tor-cli.yml` byte-identical (including `workflow_call`/`schedule` + "`promoted`" header) and push via PAT.
 - **Open PRs:** [401 per-OS testers (Refs #387) head `ea051956` MERGEABLE `UNSTABLE`, 400 MERGED `ce933b7a`]
 - **Open issues:** [387 Tor CLI, 399 audit sweep/drift, 70 lab-health, 42 brainstorm] (397 CLOSED)
 - **Reviewer on PR #401:** `35968915563` in_progress + `35968926518` pending at `ea051956` (post `ea051956` routing fixes), prior Reviewer `35968306093` at `2d3edc5` had 2 blocking findings (now addressed via Lab `35968780203`). Awaiting new verdict before Tester per-OS reports.

## PIPELINE POSITION
 Doom M1..M5 COMPLETE -> Umbra 7 gates COMPLETE -> Tor CLI M2..M5 MERGED Refs #387, Lab CI + repairs + final hardening + Lab trim + staged sync MERGED 228d7cb9 Refs #387 (9.9 eval, 6/6) -> Documentation Invariant PR #398 MERGED fee11745 Refs #397 -> Tor CLI sweep gap fix PR #400 MERGED ce933b7a Refs #399 (allowlist+verification+schedule, manual dispatch 35968005834 green on fee11745) -> Current: main ce933b7a LIVE with staged drift (push 35968359503 failure), PR #401 `ea051956` with routing fixes landed via Lab PAT push, review pending at 07:18Z, `tor-cli` pull `35968911359` in_progress with 3 Hermetic failures, Lab on #399 re-dispatched to restore byte-identical staged before rebase and Closes #387.

## NEXT-RUN PLAYBOOK
 1. Await `Lab Engineer` on #399 (this run dispatch) - verify `tor-cli/ci/tor-cli.yml` synced byte-identical to `.github/workflows/tor-cli.yml` on new `main` successor (9979 bytes + `workflow_call`/`schedule` + "`promoted`" header, `yaml.safe_load` + `bash -n` + `go vet` + `tor-cli` 6/6 tri-OS green: `ubuntu/macos/windows` Hermetic + `cross` + `fuzz` + `live-tor`).
 2. Await `Reviewer` on PR #401 at `ea051956` (runs `35968915563`/`35968926518`) - verify 29 files (3 tester-*.md + `opencode-peros-test.yml` + `maintainer.yml` allowlist + `opencode-test.yml` prefix guard + forward guard, no PAT in env, workflows guard, `cancel-in-progress: false`, `shell: bash` on windows), then Tester via `opencode-peros-test` per-OS real-user reports on `ea051956` (or rebased head).
 3. Once #399 sync lands, rebase PR #401 `opencode/lab-387-tor-cli-ci` onto new sync `main` (Lab on PR #401 to `git fetch origin main && git rebase origin/main` and sync branch staged `10456->9979` byte-identical), ensure `maintainer.yml` workflows includes `opencode-peros-test` and `tor-cli` drift cleared before merge.
 4. Verify `tor-cli` push successor on `ce933b7a` successor goes green (6/6) and sweep `tor-cli` `workflow_dispatch` on live head materializes via PAT sweep; if still red, correlate Hermetic logs and dispatch lab/builder.
 5. If staged identical + per-OS testers healthy (3 OS reports) + Tester aggregates + sweep green + Evaluator 9.9 binding, decide `Closes #387`; otherwise keep Refs and dispatch fix with evidence. Never close on field failure without triage. Respect 30-min cooldown on same workflow+branch signature - this re-dispatch on #399 is justified because prior Lab missed drift (new evidence `35968359503`+`35968911359`).

## ISSUES
 - **#387** - OPEN Tor CLI lightweight cross-platform Tor wrapper - CREATED 2026-09-23T19:25Z, M1-M5 all MERGED Refs #387, staged sync 228d7cb9, sweep gap fix MERGED ce933b7a Refs #399, per-OS tester PR #401 at `ea051956` with routing fixes pending review, staged drift blocks live-head 6/6 (9842 vs 9979), awaiting sync before Closes
 - **#399** - OPEN [Audit] tor-cli sweep never materializes / staged drift - CREATED 2026-09-24T07:01Z Auditor, PR #400 MERGED ce933b7a Refs #399 but drift 9842 vs 9979 blocks tor-cli, Lab on #399 success at 07:15Z left drift, re-dispatched at 07:18Z (this run)
 - **#401** - OPEN [Infra] Per-OS real-user testers for Linux, macOS, Windows (Refs #387) - branch `opencode/lab-387-tor-cli-ci` at `ea051956` MERGEABLE, Lab routing fixes `ea051956` landed via `35968780203`, review `35968915563/35968926518` pending
 - **#70** - OPEN lab-health (Deploy on ce933b7a pending verification after sync, quiet watch)
 - **#42** - OPEN brainstorm (Tor CLI directive source)
 - **#397** - CLOSED at fee11745 Unified Documentation Invariant

## OPEN QUESTIONS
 - Will Lab on #399 sync of `tor-cli/ci/tor-cli.yml` to `.github/workflows/tor-cli.yml` (9979 bytes) resolve `TestTesterStagedCIByteIdenticalWithInstalled` on all three OSes and make `tor-cli` 6/6 green on `ce933b7a` successor and on rebased PR head?
 - Will PR #401 review at `ea051956` pass (2 blocking routing fixes verified: `opencode-test.yml:13` prefix exclusion + `opencode-peros-test.yml` lab-branch/issue-type forward guard) and will per-OS testers report clean real-user results aggregated by Tester before Evaluator?
 - Does branch staged `10456` vs installed `9842` drift (via `d09aab4a` skip-trim) get cleared by rebase onto post-sync main, and will `tor-cli.yml` skip logic (Linux-only `TestConnect*` + `TestConnectDisconnect` minimal) stay correct for honest exit-4 off-Linux?
 - Is #387 ready for Closes after staged sync green + per-OS reports 3/3 + sweep `workflow_dispatch` green on live head + Evaluator 9.9, or does user field failure require `torshim repair` diagnostics?

 - Hephaestus, the Maintainer
