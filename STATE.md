# STATE - Random factory checkpoint
 - **Updated: 2026-09-24T07:24Z (maintainer run 35969151120 - issue_comment on PR #401 ea051956 MERGED to a2e244dd, main drift still blocks tor-cli, Lab on #399 re-dispatched on new head)**

## STANDING OWNER DIRECTIVES (active)
 - **DOCUMENTATION INVARIANT DIRECTIVE (2026-09-24T06:20Z, via #397 CLOSED at fee11745):** Enforce Unified Documentation Invariant and abolish internal milestone leakage in agent prompts - codify invariant in `AGENTS.md`/`LAB.md` (no milestone/sprint leakage in `<project>/docs/` + `<project>/README.md`), semantic phase naming (descriptive vs M1/M2), update prompts for `architect`/`builder`/`reviewer`/`maintainer`/`curator`, Lab Engineer executor. Issue #397 CLOSED at fee11745, PR #398 MERGED fee11745 Refs #397 (5 commits) - invariant now live on main, historical progress/ledger untouched.
 - **TOR CLI DIRECTIVE (2026-09-23T19:14:33Z, via #42):** Build lightweight, fast, cross-platform open-source Tor CLI - `tor-tool <app> [args]` per-app only, `tor-tool shell` isolated shell, `sudo tor-tool connect`/`disconnect` system-wide with safe restore, `status`/`version`, use existing Tor (auto-manage/detect, startup/readiness/reuse/cleanup/Ctrl-C/failures/DNS/IPv4/IPv6, never falsely claim protected), Linux/macOS/Windows behind common CLI, thorough GitHub Actions testing across 3 OS. Tracking issue #387 OPEN, M1..M5 all MERGED Refs #387, Lab CI + repairs + final hardening + staged sync MERGED 228d7cb9 Refs #387 (byte-identical staged/installed 9.9 eval, 6/6 tri-OS on 4098d08). Deploy success on 228d7cb9 35962553419 and on fee11745 35965214683 success. Manual tor-cli dispatch 35968005834 success on fee11745 (live-head green before drift). Issue #387 awaits tri-OS green on live head + per-OS testers before Closes. User field failure 2026-09-24T05:44Z + per-OS tester request 2026-09-24T06:57Z.
 - **UMBRA DIRECTIVE (2026-09-22T15:43Z, via #375 CLOSED):** Shadow Fight-inspired WebGPU Combat at `/umbra/` - 7 gates COMPLETE.
 - **DOOM DIRECTIVE (2026-09-17T21:10Z, via #362 CLOSED):** Client-Side Web Doom at /doom/ - 6 gates, issue #362 CLOSED at e33e11f1 (M1-M5 complete).
 - **POOLDUEL CLOSED (2026-09-16T16:56Z, via #302):** Owner closed #302; REDESIGN terminated.
 - **LAB RIGOR GATES (2026-09-07T15:47Z):** Brutal rigor charter live.
 - **EXCELLENCE IN CRAFTSMANSHIP CHARTER (2026-09-04T16:28Z):** Live on 61b09c8.
 - **LAB HEALTH NOTICE (2026-09-07T16:06Z via #70):** RESOLVED - README.md and index.html now accurately reflect live main.

## CRITICAL INFRASTRUCTURE STATE
 - **Main a2e244dd LIVE - drift blocks green:** `origin/main` = `a2e244ddc24e650a04946fb7220d321f785992b8` verified via `git ls-remote origin/main` == a2e244dd and `gh api repos/Userfrom1995/RandomLabs/git/refs/heads/main --jq .object.sha` == a2e244dd (parent ce933b7a, merge PR #401 `lab: fix per-OS routing double-trigger plus lab-branch forward guard` Refs #387 + Refs #399, 2 commits via PAT). `git log --oneline -5 origin/main` = a2e244dd / ce933b7a / d3fabf5a / fee11745 / 057033cb. `tor-cli` push on ce933b7a `35968359503` failure: Hermetic `ubuntu/macos/windows` FAIL `TestTesterStagedCIByteIdenticalWithInstalled` (9842 vs 9979 bytes, windows 10099 vs 10240 CRLF) while `cross-compile` success + `fuzz` pass; pull on `ea051956` `35968911359` same 3x Hermetic fail; no `tor-cli` run yet on `a2e244dd` (merge via GITHUB_TOKEN suppresses push trigger, sweep is sole validation path). `tor-cli` at `fee11745` was green via `35968005834` workflow_dispatch before drift. `maintainer.yml` workflows 18/18 PASS on main (includes `tor-cli` + `opencode-peros-test`). `opencode.json` two-knob free (`muse-spark-1.3-contributor-free` / `muse-spark-1.2-contributor-free`, evaluator `muse-spark-1.3-contributor-free`). No `workflows permission` rejection on Lab PAT pushes (35968780203 + merge a2e244dd).
 - **PR #401 per-OS testers MERGED to a2e244dd:** Branch `opencode/lab-387-tor-cli-ci` `ea051956` (5 commits: prompts, workflow 303 lines, registry+docs, tor-cli skip-trim, routing fixes) MERGED at a2e244dd via PAT. Lab Engineer push `35968780203` + merge verified via `git ls-remote`. Reviewer `35968915563` -> `/oc lab fix review findings` (2 blocking routing defects) -> Lab `35969363257` fixed -> Reviewer `35968915563` re-run `/oc approve` at ea051956 + Tester `35969021676` `/oc approve-test` infra-guard read-only (YAML parses 5 workflows, 75 run blocks bash -n, zero em dashes, no PAT in env, cancel-in-progress false, shell bash on windows, prefix guard at opencode-test.yml:13, forward lab-branch+is_pr guard at opencode-peros-test.yml:93/178/266). `opencode-peros-test.yml` now live on main with 3 jobs `ubuntu/macos/windows` native, `cancel-in-progress: false`, `GITHUB_TOKEN` only.
 - **Tor CLI epic position:** Staged sync MERGED at `228d7cb9` (9.9 eval, 6/6 on `4098d08` byte-identical). Sweep gap fix MERGED `d3fabf5a+ce933b7a` Refs #399 (allowlist adds `tor-cli`, verification polling 6x30s, `workflow_call`+`schedule 17 6 * * 1` backstop). Drift introduced: `.github/workflows/tor-cli.yml` 9979 bytes (with `workflow_call`/`schedule`) vs `tor-cli/ci/tor-cli.yml` 9842 bytes (old header + missing triggers) on main a2e244dd; `TestTesterStagedCIByteIdentical` now red on every OS until sync. Per-OS tester workflow added (303 lines, 3 jobs, `cancel-in-progress: false`, `shell: bash` on windows, `GITHUB_TOKEN` only). Docs synced at 7 locations.
 - **Documentation Invariant epic position:** Issue #397 CLOSED at fee11745, invariant live.

## IN FLIGHT
 - **Tor CLI #387 OPEN - live-head drift blocks Closes:** Issue #387 OPEN, M1-M5 + Lab + hardening + staged sync all MERGED Refs #387, Evaluator 9.9 on staged sync, manual sweep `35968005834` green on `fee11745`, sweep fix MERGED `ce933b7a` Refs #399, per-OS testers MERGED `a2e244dd` Refs #387, but staged drift blocks tri-OS green on live head `a2e244dd` (9842 vs 9979). `Refs #387` retained.
 - **Audit #399 OPEN - staged sync regression, Lab re-dispatched on new head:** Issue #399 OPEN, PR #400 MERGED `ce933b7a` Refs #399 but drift `9842 vs 9979` blocks `tor-cli` 6/6 on `a2e244dd`. Lab Engineer on #399 was dispatched at 07:15Z (`35968615980` success but drift unverified) and at 07:23Z (`35969363257` in_progress on stale ce933b7a). This run re-dispatches Lab on #399 at a2e244dd to `cp .github/workflows/tor-cli.yml tor-cli/ci/tor-cli.yml` byte-identical (including `workflow_call`/`schedule` + "promoted" header) and push via PAT.
 - **Open PRs:** [] (401 MERGED a2e244dd, 400 MERGED ce933b7a)
 - **Open issues:** [387 Tor CLI, 399 audit sweep/drift, 70 lab-health, 42 brainstorm] (397 CLOSED)
 - **Reviewer/Test on PR #401:** Both completed `/oc approve` + `/oc approve-test` at ea051956 before merge; no further PR to review.

## PIPELINE POSITION
 Doom M1..M5 COMPLETE -> Umbra 7 gates COMPLETE -> Tor CLI M2..M5 MERGED Refs #387, Lab CI + repairs + final hardening + Lab trim + staged sync MERGED 228d7cb9 Refs #387 (9.9 eval, 6/6) -> Documentation Invariant PR #398 MERGED fee11745 Refs #397 -> Tor CLI sweep gap fix PR #400 MERGED ce933b7a Refs #399 (allowlist+verification+schedule, manual dispatch 35968005834 green on fee11745) -> Per-OS testers PR #401 MERGED a2e244dd Refs #387 (routing fixes: opencode-test.yml prefix guard + opencode-peros-test.yml lab-branch/is_pr forward guard, 303-line workflow, 3 native jobs) -> Current: main a2e244dd LIVE with staged drift (push 35968359503 failure, no tor-cli run on a2e244dd), Lab on #399 re-dispatched on new head to restore byte-identical staged before tri-OS green + per-OS reports + Closes #387.

## NEXT-RUN PLAYBOOK
 1. Await `Lab Engineer` on #399 (this run dispatch on a2e244dd) - verify `tor-cli/ci/tor-cli.yml` synced byte-identical to `.github/workflows/tor-cli.yml` on new `main` successor (9979 bytes + `workflow_call`/`schedule` + "promoted" header, `yaml.safe_load` + `bash -n` + `go vet` + `tor-cli` 6/6 tri-OS green: `ubuntu/macos/windows` Hermetic + `cross` + `fuzz` + `live-tor`).
 2. Once #399 sync lands, verify `tor-cli` push successor on `a2e244dd` successor goes green (6/6) or `tor-cli` `workflow_dispatch` sweep on live head materializes via PAT sweep allowlist with run-existence polling 6x30s (fail loudly if absent); if still red, correlate Hermetic logs and dispatch lab/builder.
 3. Then dispatch per-OS real-user testers on #387: `/oc test-linux`, `/oc test-macos`, `/oc test-windows` (each native runner, every command/flag/workflow, exit contracts 0/1/2/3/4, evidence), Tester aggregates before Evaluator re-gate; `tor-cli.yml` skip logic (Linux-only syswide M3 contract) must stay honest exit 4 off-Linux.
 4. If staged identical + per-OS testers healthy (3 OS reports + Tester aggregation) + sweep green + Evaluator 9.9 binding, decide `Closes #387`; otherwise keep Refs and dispatch fix with evidence. Never close on field failure without triage. Respect 30-min cooldown on same workflow+branch signature - this re-dispatch on #399 is justified because main advanced a2e244dd (new SHA, prior Lab on stale ce933b7a).
 5. Verify Deploy `pages.yml` preview stays green after sync.

## ISSUES
 - **#387** - OPEN Tor CLI lightweight cross-platform Tor wrapper - CREATED 2026-09-23T19:25Z, M1-M5 all MERGED Refs #387, staged sync 228d7cb9, sweep gap fix MERGED ce933b7a Refs #399, per-OS testers MERGED a2e244dd Refs #387, staged drift blocks live-head 6/6 (9842 vs 9979), awaiting sync before Closes
 - **#399** - OPEN [Audit] tor-cli sweep never materializes / staged drift - CREATED 2026-09-24T07:01Z Auditor, PR #400 MERGED ce933b7a Refs #399 but drift 9842 vs 9979 blocks tor-cli, Lab on #399 in_progress on ce933b7a (35969363257 stale), re-dispatched at a2e244dd (this run)
 - **#401** - CLOSED MERGED at a2e244dd [Infra] Per-OS real-user testers for Linux, macOS, Windows (Refs #387) - branch `opencode/lab-387-tor-cli-ci` at `ea051956` MERGED, Reviewer approve + Tester approve-test verified, routing fixes live
 - **#70** - OPEN lab-health (Deploy on a2e244dd pending verification after sync, quiet watch)
 - **#42** - OPEN brainstorm (Tor CLI directive source)
 - **#397** - CLOSED at fee11745 Unified Documentation Invariant

## OPEN QUESTIONS
 - Will Lab on #399 sync of `tor-cli/ci/tor-cli.yml` to `.github/workflows/tor-cli.yml` (9979 bytes) on a2e244dd resolve `TestTesterStagedCIByteIdenticalWithInstalled` on all three OSes and make `tor-cli` 6/6 green on `a2e244dd` successor and via workflow_dispatch sweep?
 - Will per-OS testers (`opencode-peros-test` 3 jobs) report clean real-user results on #387 aggregated by Tester before Evaluator, with honest exit 4 off-Linux and no PAT in env?
 - Does `tor-cli.yml` skip logic (Linux-only `TestConnect*` + `TestConnectDisconnect` minimal) stay correct for honest exit-4 off-Linux after sync?
 - Is #387 ready for Closes after staged sync green + per-OS reports 3/3 + sweep `workflow_dispatch` green on live head + Evaluator 9.9, or does user field failure require `torshim repair` diagnostics?

 - Hephaestus, the Maintainer
