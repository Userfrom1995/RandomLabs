# Progress: Prism #130 - blend-0 full-24 floor verification (issue #130)

- **Branch:** `opencode/issue130-20260903114816`
- **Status:** in-progress
- **Date:** 2026-09-03 (Builder run, `/oc build this` trigger, resume mode)
- **Precedent:** PR #262 isolated the +1.12% default-blend regression to the
  shipped 15-64-32-1 MLP prior (blend-0 kodim01 reproduces the floor row to
  22 bytes). PR #263 (OPEN, MERGEABLE) flips baked LBlend 0.6 -> 0.0 and proves
  blend-0 quad recovers the floor (+0.025%). This run executes the queued
  follow-up: full-24 blend-0 verification of the X6b floor 3.2175/9.6525 on
  current main, sharded across runs (~5 min/image, ~2 h total).

## Binding gates (units mandatory, restated)

- M2: summed < 9.498 AND per-sample < 3.166 (vs real WebP m6 3.166)
- M3: summed < 8.655 AND per-sample < 2.885 (vs real cjxl -d0 -e9 2.885)
- `bench_gate.sh` dual-unit check is the only acceptance authority.
- `Refs #130` only, never `Closes #130` while gates fail.

## This run (Builder, 2026-09-03, resume mode - no redo of done work)

1. Oriented: read issue #130 tail, PR #263/#264 state, prior progress
   (`130-prism-floor-fresh-20260903.md`). No new mechanism class since R9;
   per resume mode, measuring rather than rebuilding.
2. Corpus: `prism/benchmarks/data/kodak` symlink, **24/24 SHA256 OK** against
   `prism/benchmarks/data/kodak.sha256` (bare-name pin file; note the
   obsidian-namespaced pin file in the same resolved dir uses `data/kodak/`
   prefixes and fails from the wrong cwd - use the prism pin file).
3. Method (identical to the committed 08-29 floor CSV for comparability):
   `prism bench-x --residual --blend 0 --kodak <shard-dir> --out <csv>`
   (LeGall 5/3, levels 5, pure EMA). Shard dirs are symlinks into the pinned
   corpus (no copies, no re-encode of sources).
4. Shard plan: A = kodim01-08, B = kodim09-16, C = kodim17-24. This run does
   shard A; shards B/C are queued for continuation runs; a final run
   aggregates wnet over all 24 and evaluates the dual-unit gate.

## Milestone Checklist

- [x] Orient + read ledger (no redo of done work)
- [x] SHA-verified corpus present (24/24 files, 0 failures)
- [x] Clean Release build from current main (`f968ef8`, blend default 0.0 baked)
- [x] Branch rebuilt onto current main (merge-base FAIL - main history rewritten; cherry-picked own scaffold commit only)
- [x] Shard A (kodim01-08) blend-0 CSV
- [ ] Shard B (kodim09-16) blend-0 CSV (NEXT RUN)
- [ ] Shard C (kodim17-24) blend-0 CSV (NEXT RUN)
- [ ] Aggregate wnet over all 24 + dual-unit gate eval (FINAL RUN)
- [ ] Commit + push shard A CSV; decision file (continue for shards B/C)

## Measurements - shard A (2026-09-03, current main `f968ef8`)

Method: `/tmp/opencode/prism-build/prism bench-x --residual --blend 0
--kodak <shard-A-symlinks> --out
prism/benchmarks/results/2026-09-03-x6b-blend0-shardA.csv`
(LeGall 5/3, levels 5, pure EMA; `--blend 0` passed explicitly even though
the baked default is now 0.0 after PR #263, for strict comparability).
Durable CSV committed:
`prism/benchmarks/results/2026-09-03-x6b-blend0-shardA.csv`.

| Image | 08-29 floor wnet | Fresh blend-0 wnet | Delta |
|---|---|---|---|
| kodim01 | 506365 | 506343 | -22 (-0.004%) |
| kodim02 | 454895 | 454984 | +89 (+0.020%) |
| kodim03 | 409186 | 409217 | +31 (+0.008%) |
| kodim04 | 468202 | 468597 | +395 (+0.084%) |
| kodim05 | 529537 | 529625 | +88 (+0.017%) |
| kodim06 | 481054 | 481072 | +18 (+0.004%) |
| kodim07 | 424027 | 424110 | +83 (+0.020%) |
| kodim08 | 547373 | 547459 | +86 (+0.016%) |
| shard-A sum | 3820639 | 3821407 | +768 (+0.020%) |

L1_shrink values are bit-identical to the committed CSV on all 8 images.
Shard-A mean per-sample 3.23945, mean summed 9.71834 (harness printout;
NOT a gate eval - M2/M3 gates apply to the full-24 aggregate only).

Floor reproduces: worst image +0.084%, shard sum +0.020%. The residual
tens-of-bytes deltas vs the 08-29 binary are consistent with code drift
since 08-29 (blend plumbing / baked-default flip); the MLP prior is fully
gated out at `--blend 0` in both. spayload (MED-residual diagnostic) deltas
are larger in places (e.g. kodim05 547501 vs 546270, +0.23%) but spayload
does not enter wnet - it only affects the deco_pct diagnostic.

## Next steps (for continuation runs)

1. Shard B: same command with `--kodak /tmp/opencode/shards/B` (recreate
   symlinks - /tmp does not persist across runs), out
   `prism/benchmarks/results/2026-09-03-x6b-blend0-shardB.csv`.
2. Shard C: same with shard C dir, out `...-shardC.csv`.
3. Final run: sum wnet over A+B+C CSVs, compare to 9.6525 floor sum,
   evaluate `bench_gate.sh` dual-unit gate (expected FAIL per ledger -
   floor needs 1.6%/11.5%), update this file to `Status: complete`.
4. `Refs #130` only, never `Closes #130` while gates fail.

- the Builder
