# Progress: Prism #130 - Blend-0 default full-24 floor confirmation (issue #130)

- **Branch:** `opencode/issue130-20260903133150`
- **Status:** complete (blend-0 default technique fully measured; gates still FAIL, no success claim)
- **Date:** 2026-09-03 (Builder run, `/oc build` trigger, resume mode)
- **Precedent:** PR #262 finding (shipped 15-64-32-1 MLP prior HARMFUL at baked
  blend 0.6: default-blend quad +1.12% vs floor, MED-residual spayload +35%).
  Prior run on `opencode/issue130-20260903103047` flipped baked default
  `LBlend 0.6 -> 0.0` (`prism/src/codec/learned_ctx_data.inc:108`, now on main)
  and verified blend-0 pinned quad recovers the floor (+0.025%). This run
  contributes the missing datum: full-24 under the new default.

## Binding gates (units mandatory, restated)

- M2: summed < 9.498 AND per-sample < 3.166 (vs real WebP m6 3.166)
- M3: summed < 8.655 AND per-sample < 2.885 (vs real cjxl -d0 -e9 2.885)
- `bench_gate.sh` dual-unit check is the only acceptance authority. `Refs #130`
  (never `Closes #130` while gates remain open).

## This run (Builder, 2026-09-03, resume mode - no redo of done work)

1. Oriented: branch == origin/main `4af1e88` (LBlend 0.0 already on main).
   Verified `default-quad.csv` is byte-identical to `blend0-quad.csv`
   (`diff` clean) - the flipped default reproduces blend-0 exactly.
2. Clean Release configure + build from current HEAD (exit 0).
3. Full-24 `bench-x --residual` (LeGall 5/3, levels 5, default blend = 0.0),
   sharded 4-way across cores on sha-pinned `prism/benchmarks/data/kodak`
   (24/24 PPMs). Durable CSV:
   `prism/benchmarks/results/2026-09-03-x6b-full24-defaultblend.csv`.
4. Targeted suite `Blend.*:R6B.*:Wavelet*:*Learned*`: **11/11 PASS**.
   (Full suite exceeds the run time budget: `R7.HeldOutVsBaseline` alone needs
   600 s+ and is red on unmodified main by design per PR #261; HEAD commit
   already records 253/253 on this exact tree.)
5. `bench_gate.sh --self-check`: PASS (gate demonstrably fails known-bad and
   passes known-good, both units printed). D1 deliverable intact.

## Fresh full-24 result (default blend 0.0, current main)

- Mean per-sample: **3.21843** bpp | Mean summed: **9.65529** bpp/img
- vs 08-29 floor (3.2175/9.6525): **+0.029% / +0.029%** - floor recovered.
- M2 (<3.166/<9.498): **FAIL** (+1.66%). M3 (<2.885/<8.655): **FAIL** (+11.6%).
- Gate eval by direct dual-unit mean comparison (same convention as
  `bench_gate.sh:18`, which drives the e7 path; bench-x CSVs carry both units
  per row). No success claim: `Refs #130` only.

## What this means (honest, no success claim)

- The blend-0 default fix is now a complete, measured technique on one line of
  reasoning: flip (prior run) + quad byte-identity (verified this run) +
  full-24 floor recovery +0.029% (this run). The shipped prior's regression is
  eliminated from the default config; the trained-prior program must gate any
  future prior on BOTH wavelet and spatial paths before changing the default.
- Remaining M2 gap 1.66% / M3 gap 11.6%: no unmeasured mechanism class
  identified; standing owner question (a)/(b)/(c) from prior escalations stands.

## Milestone Checklist

- [x] Orient + read ledger (no redo of done work)
- [x] Default-quad byte-identical to blend-0 quad (diff clean)
- [x] Clean Release build from current HEAD
- [x] Full-24 bench-x default-blend (24/24, durable CSV, both units)
- [x] Dual-unit gate eval (M2/M3 FAIL, expected, no success claim)
- [x] Targeted suite 11/11 + bench_gate self-check PASS
- [x] Commit + push; decision file

- the Builder
