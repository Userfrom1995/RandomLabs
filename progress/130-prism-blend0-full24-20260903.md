# Progress: Prism #130 - blend-0 full-24 floor verification (issue #130)

- **Branch:** `opencode/issue130-20260903114816`
- **Status:** in-progress
- **Date:** 2026-09-03 (Builder run, `/oc build this` trigger, resume mode)
- **Precedent:** PR #262 isolated the +1.12% default-blend regression to the
  shipped 15-64-32-1 MLP prior (blend-0 kodim01 reproduces the floor row to
  22 bytes). PR #263 (OPEN, MERGEABLE) flips baked LBlend 0.6 -> 0.0 and proves
  blend-0 quad recovers the floor (+0.025%). This run executes the queued
  follow-up: full-24 blend-0 verification of the X6b floor 3.2175/9.6525 on
  current main, sharded across runs (~5 min/image, ~2 h total).

## Binding gates (units mandatory, restated)

- M2: summed < 9.498 AND per-sample < 3.166 (vs real WebP m6 3.166)
- M3: summed < 8.655 AND per-sample < 2.885 (vs real cjxl -d0 -e9 2.885)
- `bench_gate.sh` dual-unit check is the only acceptance authority.
- `Refs #130` only, never `Closes #130` while gates fail.

## This run (Builder, 2026-09-03, resume mode - no redo of done work)

1. Oriented: read issue #130 tail, PR #263/#264 state, prior progress
   (`130-prism-floor-fresh-20260903.md`). No new mechanism class since R9;
   per resume mode, measuring rather than rebuilding.
2. Corpus: `prism/benchmarks/data/kodak` symlink, **24/24 SHA256 OK** against
   `prism/benchmarks/data/kodak.sha256` (bare-name pin file; note the
   obsidian-namespaced pin file in the same resolved dir uses `data/kodak/`
   prefixes and fails from the wrong cwd - use the prism pin file).
3. Method (identical to the committed 08-29 floor CSV for comparability):
   `prism bench-x --residual --blend 0 --kodak <shard-dir> --out <csv>`
   (LeGall 5/3, levels 5, pure EMA). Shard dirs are symlinks into the pinned
   corpus (no copies, no re-encode of sources).
4. Shard plan: A = kodim01-08, B = kodim09-16, C = kodim17-24. This run does
   shard A; shards B/C are queued for continuation runs; a final run
   aggregates wnet over all 24 and evaluates the dual-unit gate.

## Milestone Checklist

- [x] Orient + read ledger (no redo of done work)
- [x] SHA-verified corpus present (24/24 files, 0 failures)
- [ ] Clean Release build from current main
- [ ] Shard A (kodim01-08) blend-0 CSV
- [ ] Commit + push shard A CSV; decision file (continue for shards B/C)

## Measurements

(pending build)

- the Builder
