# Progress: Route 6D - True JXL-Modular Property Tree with Transmitted Per-Leaf Histograms (issue #130)

- **Branch:** `opencode/issue130-route6d-property-tree`
- **PR:** (to be opened) `Refs #130`
- **Blueprint:** `ideas/2026-08-29-prism-route6d-property-tree.md`
- **Research spec:** `prism/docs/research-route6d-property-tree.md` (Dr. Mob, the Researcher)
- **Precedent:** R6-A (3.2459), R6-B (3.4363), R6-C (5.08 on untrained weights) - all three Route 6
  variants failed to beat X6b. R6-D is the genuine JXL-Modular mechanism: a baked property tree over
  RAW neighbour magnitudes + per-leaf transmitted P(0) histograms, blending with the retained EMA.
- **Base floor (X6b):** 3.2442 per-sample / 9.7326 summed on real Kodak-24.
- **Status:** Blueprint delivered by the Architect. Ready for Builder (`/oc build this`).

## Milestone Checklist

### D0: Scaffold + baked tree
- [ ] `route6d_tree.inc` with `R6D_TREE`, `R6D_K=2048`, `R6D_FEAT_*` (generated by trainer)
- [ ] `r6d_raw_features` + `r6d_leaf` (+ `r6d_raw_feat`) in `bitplane.cpp`; `R6DRaw`/`R6DNode` in `bitplane.h`
- [ ] `R6D_FLAG = 16`, `WaveletHeader::r6d_k` / `r6d_p0`; serialize/parse in `wavelet_container.cpp`

### D1: Two-pass static-tree coder
- [ ] `encode_static_tree` / `decode_static_tree` + `StaticTreeModel` / `StaticTreeHist` / `StaticTreeBitplaneResult`
- [ ] `frame_wavelet_encode_r6d` + decode dispatch (`R6D_FLAG`)

### D2: CLIs + offline trainer
- [ ] `prism wavelet-r6d`, `prism bench-r6d` (dual-unit CSV + byte-exact + fuzz)
- [ ] `prism train-r6d-tree` (extends `collect_samples` for raw magnitudes; greedy growth; bakes .inc)

### D3: Tests
- [ ] `tests/unit/test_r6d.cpp` (full-frame, subband, variants, no-worse, determinism, fuzz) registered in `prism/CMakeLists.txt`

### D4: Measurement + gates
- [ ] R6-D0 held-out 4-image (kodim02/07/17/21) median NET <= -2.0% vs X6b (3.2442) BEFORE full run
- [ ] R6-D1 full Kodak-24: summed <= 9.498 AND per-sample <= 3.166 (M2), byte-exact, fuzz clean
- [ ] R6-D2 sweep K/W; add R-class refinement histogram / stack X6c `sub_scale` if short
- [ ] R6-D3 full Kodak-24 dual-unit: summed <= 8.655 AND per-sample <= 2.885 (M3)

## Binding gates (restated, units mandatory)
- M2: summed < 9.498 AND per-sample < 3.166 (vs WebP m6 3.166)
- M3: summed < 8.655 AND per-sample < 2.885 (vs real cjxl -d0 -e9 2.885)
- `bench_gate.sh` dual-unit check is the only acceptance authority. `Refs #130` (never `Closes #130`
  while gates remain open).

## Cascade (honest, no re-tuning to force a pass)
- R6-D0 FAIL: property-tree / transmitted-histogram lever exhausted on this residual -> STOP-AND-REPORT,
  escalate to Owner for Route 7 (transform/prediction redesign).
- R6-D0 PASS, R6-D1 M2 PASS, R6-D3 M3 FAIL: M2 genuinely PASS (first in lab history); M3-PENDING;
  attempt R6-D2 stacking, then escalate for Route 7.
- R6-D3 PASS: both gates in both units -> format-stable v3 PR `Refs #130`.

- the Architect
