# Progress: Prism #130 - Tree-health verification (issue #130)

- **Branch:** `opencode/issue130-20260903091209`
- **Status:** escalates to Maintainer - tree healthy, floor re-confirmed, no open lever
- **Date:** 2026-09-03 (Builder run, `/oc build` trigger)
- **Precedent:** X6b floor 3.2175 per-sample / 9.6525 summed. M2 gap 1.63%. M3 gap 11.53%.
  9+ programs / 49+ phases measured and rejected with committed CSVs. Prior runs
  (2026-09-02 continue, 2026-09-03 fresh escalation + retry-confirmed + deterministic
  re-confirm) all escalated with the same standing owner question.

## This run (Builder, 2026-09-03, resume mode - no redo of done work)

1. Oriented to issue #130. Reviewed the negative ledger and the last four escalation
   runs. No new mechanism class proposed since; per resume mode, verified rather
   than rebuilt.
2. Fresh configure + full Release build from `origin/main` (`7c6b8ba`): compiles
   clean (only pre-existing `-Wunused` warnings in `prism/src/cli/main.cpp`).
3. `bench_gate.sh --self-check`: PASS (gate demonstrably fails known-bad and passes
   known-good input, both units printed). D1 deliverable intact.
4. Full unit suite `prism_tests`: **260/261 pass**. The single failure is the known
   pre-existing `R7.HeldOutVsBaseline` guard (experimental R7-A path ~14% worse than
   X6b on held-out photos; exists on unmodified main; test skips vacuously without
   corpus data). No gate-gutting applied - that belongs to Route 7's open PR #186.
5. kodim01 e7 spot check (`enc --effort 7` / `dec`): **3.6502 bpp, `cmp`-verified
   byte-exact**, bit-identical to the value recorded by the prior deterministic
   re-confirm run. Encoder is deterministic; the floor reproduces exactly.
6. Solo re-run of `R7.HeldOutVsBaseline` was abandoned after 600 s (the test runs the
   full held-out measurement; the full-suite result already records its red state).

## Honest state (dual units, unchanged)

| Path | Per-sample | Summed | M2 | M3 |
|------|-----------|--------|----|----|
| Standard e7 (spot-checked, this run) | 3.6502 (kodim01) | - | FAIL | FAIL |
| X6b wavelet+EMA floor | 3.2175 | 9.6525 | FAIL (+1.6%) | FAIL (+11.5%) |
| Oracle (cheat) | 3.161 | 9.483 | barely PASS | FAIL (+9.6%) |

## Escalation

Per builder.md escalation protocol: `{"action":"maintainer"}`. Same standing
question for the Owner: (a) accept 3.2175/9.6525 as the honest best and close
#130, (b) authorize a fundamentally new architecture with GPU + large-corpus
training infrastructure, or (c) relax the binding gates. Per Anti-Surrender +
No-Pause, #130 stays OPEN; `Refs #130` only, never `Closes #130` while gates fail.

## Binding gates (units mandatory, restated)

- M2: summed < 9.498 AND per-sample < 3.166 (vs real WebP m6 3.166)
- M3: summed < 8.655 AND per-sample < 2.885 (vs real cjxl -d0 -e9 2.885)

- the Builder
